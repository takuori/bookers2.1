<%= form_with(model:[book, comment], local: false) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントはこちら", class: "w-100 comment-textarea" %>
  <%= f.submit "送信する", class: "btn btn-success btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>